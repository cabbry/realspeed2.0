<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIDeviceFamily</key>
	<array>
		<integer>1</integer>
		<integer>2</integer>
	</array>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>CFBundleIdentifier</key>
	<string>com.cabbry.realspeed2</string>
	<!-- CFBundleShortVersionString and CFBundleVersion are injected from the .csproj
	     (ApplicationDisplayVersion / ApplicationVersion). In CI, ApplicationVersion
	     is overridden by GITHUB_RUN_NUMBER so each build gets a unique increment. -->
	<!-- Pre-answer the App Store "encryption compliance" question so each TestFlight
	     upload is not blocked waiting for manual validation. The app uses only the
	     OS-provided HTTPS (exempt) and no custom cryptography. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>We need your location to compute your real-time speed.</string>
	<key>XSAppIconAssets</key>
	<string>Assets.xcassets/appicon.appiconset</string>
	<!-- iOS 26 "Liquid Glass" opt-out: render with the pre-iOS-26 design language
	     so MAUI controls don't get the new translucent material overlays.
	     Remove this once the UI is intentionally updated for Liquid Glass. -->
	<key>UIDesignRequiresCompatibility</key>
	<true/>
</dict>
</plist>
